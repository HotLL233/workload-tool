version: "3.8"
services:
  workload-tool:
    build: .
    image: workload-tool:v0.4.24
    container_name: workload-tool
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - WORKLOAD_DATA_DIR=/app/data
